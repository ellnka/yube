import { FilterResultsDirective } from './filter-results.directive';

describe('FilterResultsDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterResultsDirective();
    expect(directive).toBeTruthy();
  });
});
